<UserControl x:Class="MSMQCommander.Views.MessagesGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300" 
             cal:Bind.Model="MSMQCommander.ViewModels.MessagesGridViewModel">
    <Grid>
        <DockPanel>
            <DataGrid ItemsSource="{Binding Path=Messages}" 
                      SelectedItem="{Binding Path=SelectedItem, Mode=OneWayToSource}"
                      AutoGenerateColumns="False" AlternatingRowBackground="Lavender">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Label" IsReadOnly="True" MinWidth="100" Binding="{Binding Path=Label}"/>
                    <DataGridTextColumn Header="Sent time" MinWidth="80"  Binding="{Binding Path=SentTime}"/>
                    <DataGridTextColumn Header="Id" Width="*" Binding="{Binding Path=Id}"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>
